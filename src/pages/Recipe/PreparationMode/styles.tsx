import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  itemName: {
    marginBottom: 10,
    flexWrap: 'wrap',
    flex: 1,
    fontSize: 16,
    fontWeight: '500',
  }
});

export default styles;
